[package]
name = "mds_app"
version = "0.1.0"
edition = "2021"

[dependencies]
mds_domain = { path = "../mds_domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_path_to_error = "0.1"